node_modules
dist
.output
.wrangler
.vinxi
.nitro
.tanstack
.vercel
.grok
screenshots
artifacts
*.log
.DS_Store
.env
.env.*
!.env.example

# Cloudflare Worker secrets for local `wrangler dev`, and the local SQLite
# database `vite dev` writes. Neither belongs in the repository.
.dev.vars
.dev.vars.*
!.dev.vars.example
.data
.node_modules.lock
.preview
*.local
